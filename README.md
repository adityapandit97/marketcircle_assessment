# README
Clone the repository.


Setup 

  Install ruby 3.0.0 
  Move inside project Directory `cd Marketcircle`.
  Run`bundle install`
  Run `rails db:create`
  Run `rails db:migrate`
  Run `./bin/dev`

